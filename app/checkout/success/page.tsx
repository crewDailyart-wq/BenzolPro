import OrderSuccess from "@/components/OrderSuccess";

export const metadata = {
  title: "Bedankt voor je bestelling — BenzolPro",
  robots: { index: false },
};

export default function SuccessPage() {
  return <OrderSuccess />;
}
