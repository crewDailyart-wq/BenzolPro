import Checkout from "@/components/Checkout";

export const metadata = {
  title: "Afrekenen — BenzolPro",
  robots: { index: false },
};

export default function CheckoutPage() {
  return <Checkout />;
}
